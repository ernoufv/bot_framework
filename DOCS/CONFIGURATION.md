# Project configuration

The whole bot configuration can be edited in ./bot/.env :
- Database connection
- Medias directories
- Default fallback actions
- NLP engine used and it's configuration
- Messenger platform configuration
